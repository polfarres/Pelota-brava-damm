# Microsoft Office temp / lock files
~$*

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor/IDE local state
.vscode/
.idea/
.claude/
*.swp
*.swo

# Python (for the upcoming backend/)
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.local
*.egg-info/
.pytest_cache/

# Node / Next.js (for the upcoming frontend/)
node_modules/
.next/
out/
.turbo/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build / cache artefacts
dist/
build/
*.log

# Processed parquet files ARE committed (locked decision A-19).
# Geocode cache IS committed too — saves the team from re-hammering Nominatim
# for 1,200 addresses on every fresh clone.

# One-off scratch directory used by emitter smoke tests during dev.
backend/data/test_emitter_output/

# Demo runner output — regenerate locally before pitching, not committed.
backend/data/demo_output/
