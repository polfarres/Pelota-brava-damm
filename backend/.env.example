# Backend environment variables — copy to .env and fill in.

# Optional: contact for the User-Agent header used when calling Nominatim.
# Nominatim's usage policy requires a real contact in the User-Agent.
NOMINATIM_USER_AGENT=smart-truck/0.1 (replace-with-team-contact@example.com)
