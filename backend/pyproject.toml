[project]
name = "smart-truck"
version = "0.1.0"
description = "DAMM DDI Smart Truck — joint route + load optimisation for Mollet warehouse"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py310"
