"""Paperwork: parse source DDIDGP PDFs and emit Smart Hoja Carga / Ruta.

Modules:

- ``parser`` — read existing ``Hoja Carga.pdf`` and ``Hoja Ruta.pdf`` from
  ``Hackaton/DAMM/RECURSOS/`` into structured objects (FR-004).
- ``emitter`` — render the Smart variants via ReportLab (FR-010, FR-011).
"""
